@model CheckBox

<div class="form-check">
    <input class="form-check-input" type="checkbox" value="" id="input-@Model.Name" @(Model.Value ? "checked" : null)>
        <label class="form-check-label" for="input-@Model.Name">
        @Model.Name
    </label>
</div>